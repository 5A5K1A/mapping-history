var oApp = null;
document.addEventListener("DOMContentLoaded", function(event) {

	// create app instance
	var elContainer = document.getElementById('maphist_app');
	oApp = new MapHist.App(elContainer);

});
